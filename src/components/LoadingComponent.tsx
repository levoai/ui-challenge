import React from 'react';
import '../App.css';

export default function LoadingComponent() {
    return (
        <main>

            <br/>
            <div>Loading...</div>
        </main>
    );
}
